sessionStorage.clear()

const myClient = {
    first_name: "Holly",
    last_name: "A",
    address: "123 Streetname",
}
 
const myTransaction = [
    
]

sessionStorage.clientInfo = JSON.stringify(myClient)
sessionStorage.cart = JSON.stringify(myClient)